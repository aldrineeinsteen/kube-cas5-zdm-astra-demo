# Example environment configuration for kube-cas5-zdm-astra-demo
# Copy this file to .env and update with your actual values

## Cassandra connection settings for the source cluster
CASSANDRA_CONTACT_POINTS=cassandra-svc
CASSANDRA_PORT=9042
CASSANDRA_USERNAME=cassandra
CASSANDRA_PASSWORD=cassandra

## Astra DB connection settings
# Provide the path to your Astra Secure Connect bundle. This file is downloaded from the Astra DB console.
ASTRA_SECURE_BUNDLE_PATH=/path/to/your/secure-connect-bundle.zip
ASTRA_TOKEN_FILE_PATH=/path/to/your/astra-token.json
# Note: Using token.json file approach - individual client credentials not needed

## Keyspace and table used by the demo
KEYSPACE=demo
TABLE=users

## Number of rows to generate in the data generator job
ROW_COUNT=1000

## ZDM Proxy configuration
# ZDM proxy version to build from source (GitHub release tag)
# See: https://github.com/datastax/zdm-proxy/releases
ZDM_VERSION=v2.3.4
